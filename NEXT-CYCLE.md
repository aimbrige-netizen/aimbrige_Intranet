# 다음 사이클 — 스펙 15~18 착수 계획

*작성 2026-08-01 · 스펙 01~14 구현·검증 완료 시점*

네 스펙을 각각 코드베이스와 대조해 조사하고, "지금 가능하다"는 판정을 다시
반박해 봤습니다. 그 결과 **지금 착수 가능한 것은 스펙 16 하나**입니다.

---

## 순서

| 스펙 | 상태 | 이유 |
|---|---|---|
| **16 커뮤니티(동호회)** | 🟢 지금 가능 | 외부 키·양식·법적 확인이 하나도 없다. 정책 결정 몇 개만 하면 된다 |
| **18 관리자 대시보드** | 🟡 6개 위젯 중 5개 | 온보딩 위젯만 막힘(스펙 07 미구현). 마지막에 둬야 관리 진입점을 한 번에 정리한다 |
| **17 AI 결재요약** | 🔴 결정 필요 | API 키보다 **개인정보 국외이전 판단**이 먼저다(아래 참고) |
| **15 이러닝** | 🔴 스펙 08과 묶어야 | 08의 교육 슬라이스를 통째로 만들어야 얹을 수 있는데, 그 부분이 노무사 확인에 걸려 있다 |

### 스펙 15를 "지금 가능"에서 내린 경위
조사 단계에서는 "08이 막힌 진짜 이유는 증명서·급여명세서 쪽이라 교육과 무관하다"고
판단했습니다. 반박 단계에서 뒤집혔습니다 — HANDOFF 3-4의 제목이 애초에
"스펙 08(**증명서·법정교육**) 차단 항목"이고, 네 항목 중 둘(법정교육 6종 노무사 확인,
교육 이수 알림 스케줄러)이 교육 항목입니다. 인덱스 문서도 "15·16·17은 확장 대상 스펙과
같은 세션에서 함께 작업"을 권고하는데, 15의 확장 대상인 08은 한 줄도 구현돼 있지 않습니다.

---

## 🔴 착수 전에 결정하셔야 하는 것

### 1. 스펙 17 — 임직원 개인정보의 국외 이전 (가장 중요)
결재 문서를 AI로 요약하는 기능인데, **요약 대상에 특별처리신청서 자유서술이
들어갑니다 — 경조사·건강·가정사**가 그대로 미국 소재 Anthropic API로 나갑니다.

- 코드로 우회할 수 없습니다.
- 한 번 전송된 내용은 회수할 수 없습니다.
- 판단 결과에 따라 **대상 문서유형 자체가 바뀝니다**(예: 특별처리신청서 제외).
  그래서 프롬프트를 설계하기 전에 답이 필요합니다.

사내 방침·고지 절차를 정하신 뒤에 착수하는 게 맞습니다. API 키는 그다음 문제입니다.

### 2. 스펙 16 — 동호회 해산 시 게시글 처리
지금 `boards`의 FK가 `on delete cascade`라 게시판을 지우면 글이 통째로 날아갑니다.
그런데 기존 `deleteBoard`는 **정확히 그 사고를 막으려고** 글이 1건이라도 있으면
삭제를 거부합니다. "부적절한 동호회를 지운다"와 정면으로 충돌합니다.

셋 중 하나를 골라야 합니다: 글까지 함께 삭제 / 글이 있으면 해산 불가 /
보관 상태로 전환(목록에서만 감춤).

### 3. 스펙 15 — 스펙 08과 함께 갈지
15만 따로 하려면 08의 교육 슬라이스(테이블 2개 + `/hr/training` + `/admin/hr`)를
먼저 세워야 합니다. 08 전체를 착수할 때 같이 하는 게 재작업이 없습니다.

---

## 착수하면 바로 걸릴 함정 (미리 적어둠)

- **(16 · critical)** `can_post_to_board()`에 community 분기를 안 넣으면 권한이 정확히
  뒤집힌다 — 가입한 직원은 자기 동호회에 글을 못 쓰고, 가입 안 한 팀장·관리자는
  아무 동호회에나 쓴다. `posts_insert` RLS 전부가 이 함수 하나에 걸려 있다.
- **(17 · critical)** `approval_documents`에는 SELECT 정책만 있다. 서버 액션에서
  `.update({ai_summary})`를 하면 오류가 아니라 **0행으로 조용히** 끝나 요약이 떴다가
  새로고침하면 사라진다. SECURITY DEFINER 함수가 필수다.
- **(17→18 · critical)** `approval_docs_touch_updated_at`이 조건 없이 걸려 있어서,
  요약을 저장하면 그 문서의 `updated_at`이 '지금'으로 튄다. 결재 평균 처리일이
  `updated_at - created_at`이라 **끝난 문서를 열어보기만 해도 통계가 늘어난다.**
- **(18 · critical)** 전사 연차 집계를 `leave_balances`로 내면 안 된다. `accrued_days`는
  그 직원이 자기 화면을 열어야 채워지는 lazy write 캐시라, 한 번도 안 들어온 직원이
  0으로 잡혀 회사 평균이 실제보다 높게 나온다.
- **(15 · critical)** 스펙 15 §4의 `ALTER TABLE` 3줄은 첫 줄에서 죽는다. 대상 테이블이
  없다. 그리고 이수 기록은 법정 기록인데 08의 RLS 원칙에 UPDATE 정책도 가드 트리거도
  없어서, 남의 이수를 만들거나 이수 시각을 지어낼 수 있다.

---

## 레일은 그대로 11칸

15~18은 **새 모듈을 0개 더합니다.** 평가를 업무 안에 넣고 자산·복지를 한 칸에 묶은
결정과 일관되게, 15는 근태 모듈로·16은 게시판 모듈로·18은 시스템 모듈로 들어갑니다.

## 마이그레이션 번호

네 스펙이 전부 24를 주장합니다(현재 최신 23). **24=커뮤니티 · 25=교육 · 26=AI요약 ·
27=대시보드**로 고정합니다. 겹치면 새 환경을 재구축할 때 실행 순서가 무너집니다.

## 순서가 중요한 이유

- **16을 먼저** — 첨부 업로드 로직이 결재·게시판·프로젝트에 이미 3벌 복사돼 있다.
  16이 그 계열을 여는 김에 공통 훅으로 빼면, 15의 수료증 업로드가 네 번째 사본이
  되는 걸 막고 기존 3화면 회귀 검증도 한 번만 한다.
- **17을 18보다 먼저** — 17이 고치는 `updated_at` 트리거가 18의 결재 통계 전제다.
- **18을 마지막** — 15의 `/admin/hr`, 16의 `/admin/community`가 확정된 뒤라야
  관리자 진입점을 한 번에 정리한다.
