# 다우오피스 채집 — 셸 확장 요소 (앱런처·레일 확장·하단 버튼·우측 유틸바·상단바 우측)

*2026-08-02 · DevTools 실측. 색·치수 출처는 06-tokens-raw.md.*

## 레일 상단 앱 런처

- 트리거: 레일 최상단 (16,79) **32×32 아이콘 버튼**(.gnb-head-trigger), 글리프 20px, 3×3 점 그리드 모양
- 클릭 → **전체 메뉴 오버레이**:
  - fixed, 상단바(60) 아래 전체 높이, z-index ~1001
  - **흰 패널 폭 1040px**, 좌측 고정, 우측으로 그림자 `rgba(20,30,50,0.2) 6px 0 24px -6px`
  - 패널 밖(오른쪽 나머지)은 어둡게(스크린샷: 반투명 딤) — 클릭 시 닫힘, ESC 닫힘
  - 헤더 줄(h~68): X 닫기 버튼 + "전체 메뉴"(18~20/600) + "사용 가능한 앱 보기" 토글 스위치 + 우측 **앱 검색 인풋**(radius 있는 필드, placeholder "앱 검색")
  - 본문 `.group_columns`: 열 폭 **400 / 400 / 240**
    - 1열: "임직원포털" 섹션 제목 **14/500 rgb(51,51,51)** + 앱 행 2열 배치
    - 앱 행: **h-36 · 14px · 아이콘(둥근 사각 틴트 배경 안 20px) + 라벨**, hover 옅은 회색
    - 3열(240): "가이드" / "관리자" / "Link+" 섹션 — 항목에 외부링크(↗) 아이콘, 섹션 사이 구분선
- 우리 매핑: 1열 = 우리 모듈 17개(2열 그리드), 관리자 열 = /admin 링크들. 앱 검색은 라벨 필터.

## 레일 하단 고정 버튼 (ul.footer.group_fixed_btn)

- 스크롤 목록과 분리된 **하단 고정 영역 h-80**, 32×32 아이콘 버튼 2개 세로 스택(글리프 20)
  1. **≡ 레일 확장 토글** (아래 '확장 모드')
  2. **조직도** (.gnb-foot-org-li) — 조직도 팝업/화면으로
- 스크롤이 넘칠 때 위/아래에 `gnb-band` 화살표 밴드(h-24, #7a7b7d)가 뜬다 — 숨긴 스크롤바 대신 "더 있음" 신호

## 레일 확장 모드 (gnb-wide)

- 폭 **64 → 200px**, 배경 **동일 #ebf4f6** (다크 아님)
- 항목이 세로 타일 → **가로 행 173×32 · radius 8** (아이콘 24 + 라벨 14/500 rgb(50,51,51))
- 활성 행: **행 전체 #08a7bf 채움 + 흰 라벨** (radius 8)
- hover: `.gnb-wide .Menu:hover { background: #cdeaf1 }` (행 전체)
- 본문 시작점도 200+260으로 밀림. 상태는 세션 지속(localStorage 급)

## 우측 유틸바 (원본 이름 organizer, --doa-organizer-w: 4rem)

- **폭 64px · 흰 배경 · 상단바 아래 전체 높이 · 화면 우측 고정** (경계선 없음 — 흰 시트와 색 경계)
- 상단: 20px 아이콘(패널 접기 ⟩|) at y82 · 32px 버튼 y124(노트/메모 — 스크린샷의 초록 배지 카드) · 32px 버튼 y197(+)
- 하단 고정: 32px 버튼 2개 (y809 파랑 다이아(메신저/AI?) · y845 ⚙설정)
- 우리 매핑: 접기 토글(유틸바 숨김) · 메모(준비중 툴팁) · +(빠른 등록 → /approvals/new) · ⚙(/admin 또는 /profile)

## 상단바 우측 클러스터 (스크린샷 + 부분 실측)

- 검색창 오른쪽 순서: **상세검색 필터 아이콘(검색창 안 우측, 20px)** → 아이콘 버튼 나열(20px 글리프/32~40 히트영역): 쪽지, 설정, 조직도, 선물(▾), 사운드, **알림 벨(+빨간 배지 #ff502a, 숫자 흰 글자)**, 도움말(? ▾) → **"앱 다운로드" 아웃라인 필**(h-40, radius 20, border #e4e5e5, ↓아이콘+13~14px) → **"경영업무포털" 시안 채움 필**(h-40, radius 20, bg #08a7bf, 흰 글자) → **아바타 원형 36**
- 우리 매핑: 쪽지→Gmail(기존), 설정→/admin(관리자만), 조직도→/directory, 알림 벨(기존), 도움말→툴팁 준비중, 앱 다운로드 자리→(기능 없음, 시각 재현) "인트라넷 안내" 또는 생략 판단, 경영업무포털 자리→관리자 콘솔(/admin) 시안 필 버튼, 아바타→프로필 메뉴(기존 확장)
- 프로필 클릭 팝업: 흰 카드 radius 12 shadow-pop, 상단 아바타+이름+이메일, 행: 내 프로필 / 개인 설정 / 로그아웃

## 모듈 화면 본문 = 전면 흰 시트 (중요)

- `.go_content`: **x=324부터 화면 끝까지 배경 #fff** (카드 아님 — 시트 전체가 흰색)
- 회청 canvas는 **홈 대시보드에만** 보인다. 패널(260, 투명)과 흰 시트의 색 경계가 "나누는 줄"로 보인다
- 우리 AppShell: 패널 있는 모듈 화면 = 본문 래퍼를 bg-surface 전면 시트로, 홈 = 기존 canvas 유지
- 흰 시트 위에서는 .ab-card 테두리 카드가 아니라 **섹션 제목 + 내용 직접 배치**가 원본 문법

## 로고/워드마크

- 원본: 검정 심볼 + "다우오피스" 굵은 검정 워드마크(로고체, 본문보다 무거움)
- 우리: **자체 제작 로고** — AB 브리지 심볼(SVG, 먹색 #1c1c1c) + "에임브릿지" 워드마크(Pretendard 700, 로고 한정 예외 굵기, 17~18px, -0.02em)
