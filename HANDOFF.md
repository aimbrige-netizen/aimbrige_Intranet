# 사용자 확인·작업 필요 항목

*최종 갱신: 2026-08-01 · 마이그레이션 20~23 적용·검증 완료*

기존 `PENDING.md`(스펙 01~06 시점)는 그대로 두고, 그 이후 쌓인 것만 여기 모았습니다.
**위에서부터 순서대로** 하시면 됩니다.

---

## 1. ✅ 마이그레이션 20~23 — 실행·검증 완료 (2026-08-01)

| 파일 | 검증 |
|---|---|
| `20260731000020_mail_widget.sql` | CHECK 3건 — 메일 위젯 껐다 켜기 동작 |
| `20260731000021_reviews.sql` | e2e 28건 |
| `20260731000022_assets_welfare.sql` | e2e 54건 |
| `20260731000023_wiki.sql` | e2e 51건 |

**e2e 197건 + 메일 파싱 9건 + CHECK 3건, 전부 통과.**

```bash
npm run e2e:approval -- --yes   # 20건
npm run e2e:storage  -- --yes   # 4건
npm run e2e:worklog  -- --yes   # 31건
npm run e2e:reviews  -- --yes   # 28건
npm run e2e:assets   -- --yes   # 54건
npm run e2e:wiki     -- --yes   # 51건
npm run check:mail              # 9건
```

닫힌 것 중 중요한 자리:
- 본인이 자기 팀장평가를 "완료"로 위조하거나, 팀장이 팀원 자기평가를 대신
  제출해 못박는 경로(구현 중 실제로 뚫려 있었음)
- 복지포인트 승인이 잔액 초과로 실패했을 때 used_points가 부분 적용되어 남는 것
- 위키 본문의 `javascript:`·`//evil.com` 피싱 링크

화면도 실제로 눌러 확인했습니다 — 자산 등록→대여 신청→승인,
복지포인트 지급→신청→승인, 위키 작성→편집→렌더까지 전 구간.
**그 과정에서 만든 데이터(위키 1건·자산 1대·2026년 복지포인트 13명분)는 전부
지웠습니다.** 복지포인트는 제가 넣은 임의값(50만점)이라 남겨두면 설정이 끝난
것으로 오해할 수 있어 지웠습니다 — 아래 2-4에서 직접 정하셔야 합니다.

> ⚠️ 검증에 `rnjsxogud2165@gmail.com`(권태형) 계정으로 임시 로그인 세션을
> 만들었다가 지웠습니다. 그 결과 이 계정의 `employees.auth_user_id`가 잠시
> 비어 있는데, **다음 번 구글 로그인 때 자동으로 다시 연결됩니다**(로그인
> 콜백이 재연결합니다). 따로 하실 일은 없고, 로그인 한 번 하시면 됩니다.

---

## 1-1. ✅ 마이그레이션 24 — 실행·검증 완료 (스펙 16 동호회)

e2e 65건 통과, 기존 e2e 188건 회귀 없음, 화면 조작 확인 완료.
데모 동호회 4개(보관 1개)·가입 13건·글 4건이 들어가 있습니다.

<details><summary>실행 전 주의사항이었던 내용 (기록용)</summary>



`supabase/migrations/20260731000024_community.sql`을 **코드 배포와 같은 시점에**
실행해야 합니다. 이 마이그레이션은 `boards`에 `description`·`created_by`·
`archived_at` 세 컬럼을 더하는데, 게시판 화면이 그 컬럼들을 함께 조회합니다.

실행 전 상태에서 코드만 올라가면 증상이 **오류 화면이 아니라 정상적인 빈
화면·404로** 보입니다:

- `/board` → "개설된 게시판이 없습니다" (조회가 통째로 실패해 빈 배열)
- 기존 `/board/{게시판}/{글}` 링크 → 전부 404
- `/community`, `/admin/community` → `list_communities` 함수 없음

화면만 보고는 원인을 알 수 없어서, 조회 실패를 서버 로그에 남기도록
`getBoard()`에 로그를 넣어 뒀습니다(`[boards] 게시판 조회 실패: column
boards.description does not exist`). 이 줄이 보이면 마이그레이션 미적용입니다.

실행 뒤 `npm run seed:demo`를 한 번 더 돌리면 데모 동호회 4개(보관 1개)와
가입 명단·글이 들어갑니다.

</details>

> ⚠️ `npm run seed:demo`가 **부서장·팀장을 데모 직원으로 덮어씁니다.**
> 실제 지정을 마치신 뒤에는 시딩을 다시 돌리지 마세요.

---

## 1-2. ✅ 마이그레이션 25 — 실행·검증 완료 (스펙 18 관리자 대시보드)

`/admin`이 전사 통계 대시보드가 됐습니다. e2e 82건 통과, 기존 253건 회귀 없음.

집계 함수 7개는 전부 관리자 전용입니다 — 일반 직원·팀장이 호출하면 거부됩니다.
`/admin` 화면 자체도 `requireSystemAdmin()`으로 막혀 있습니다.

> ✅ **결재 통계는 마이그레이션 26에서 `/approvals`까지 통일했습니다.**
> 관리자·개인 두 화면이 `approval_activity_core()` 하나를 공유하므로
> 정의가 갈릴 수 없습니다.

---

## 1-3. ✅ 마이그레이션 26 — 실행·검증 완료 (결재 통계 통일)

`/approvals`의 개인 통계가 관리자 대시보드와 같은 기준이 됐습니다.
e2e 336건 통과.

고친 것 셋: 평균 소요가 `updated_at` 기준이던 것, 임시저장이 '결재 완료'로
세어지던 것, 기간 필터가 '임시저장 만든 시각' 기준이던 것.

> ⚠️ **데모 데이터를 한 번 보정했습니다.** 시드가 `processed_at`을 스크립트를
> 돌리는 PC 시계로 써서, 결재 시각이 상신 시각보다 0.23초 앞선 행이 5개
> 있었습니다. 그러면 소요가 음수가 되어 평균에서 빠지고, 화면에는
> 표에 "결재까지 방금"인데 카드는 "결재가 끝난 문서가 없습니다"가 뜹니다.
> 시드(`seed-demo.ts`)와 기존 5행을 모두 고쳤습니다 — 지금은 평균 0.3일로
> 표와 카드가 같은 값을 냅니다.
> 운영 경로(`process_approval_step`)는 양쪽 다 DB 시계라 이 문제가 없습니다.

---

## 2. 결정이 필요한 것

### 2-1. 결재 최종승인자 (계속 대기 중)
결재라인 5종의 최종 결재자가 전부 **에임브릿지 관리자** 계정입니다.
자가승인 방지 로직 때문에 **그 계정으로는 기안을 올릴 수 없습니다**.
실제 임직원 계정 하나를 최종 결재자로 두거나, 관리자 전용 우회 규칙이 필요합니다.

### 2-2. 팀장·부서장 지정 (계속 대기 중)
`/directory`에서 팀장·부서장이 아직 아무도 지정돼 있지 않습니다.
이게 비어 있으면 이번에 만든 것 중 다음이 **화면은 뜨는데 내용이 빕니다**:

- `/worklog`, `/goals`의 팀원 조회 드롭다운
- `/reviews/[cycleId]/team` 팀원 평가 — "담당 팀원이 없습니다"
- 목표 승인 절차 전체

평가 사이클을 시작하기 전에 먼저 지정하셔야 합니다.

### 2-3. 첫 평가 사이클
`/admin/reviews`에서 사이클을 만들어야 임직원이 `/reviews`에 들어갈 수 있습니다.
기간(시작일·종료일)을 넣으면 자기평가 화면의 참고자료(해당 기간 업무일지)가
그 기간으로 걸러집니다. 비워두면 최근 것만 보입니다.

### 2-4. 복지포인트 연초 지급
`/admin/welfare`에서 1인당 금액을 넣고 일괄 지급해야 `/welfare`가 열립니다.
지급 전에는 임직원 화면에 "아직 지급되지 않았습니다"만 뜹니다.
**두 번 눌러도 두 배가 되지 않습니다** — 더하기가 아니라 그 해 지급액을
입력값으로 맞춥니다.

---

## 3. 외부 서비스 — 키·설정이 필요한 것

### 3-1. Gmail 스코프 (스펙 11)
상단바 메일 아이콘은 **지금도 동작합니다**(그냥 Gmail을 여는 링크).
대시보드의 "안읽은 메일 5건" 위젯만 스코프가 필요합니다.

1. Google Cloud Console → 데이터 액세스 → `https://www.googleapis.com/auth/gmail.readonly` 추가
2. `.env.local`의 `NEXT_PUBLIC_GOOGLE_EXTRA_SCOPES=` 에 위 주소를 넣기
3. dev 서버 재시작 후 다시 로그인(동의 화면이 한 번 더 뜹니다)

지금은 위젯이 "메일 연동이 아직 열려 있지 않습니다"로 떠 있습니다.

⚠️ 알려진 제약: Supabase의 provider_token은 약 1시간 뒤 사라집니다.
그때는 위젯에 "다시 연결" 버튼이 뜹니다. 근본 해결에는 refresh token 보관이
필요한데, 유출 시 임직원 메일함이 열리는 위험이 있어 별도 검토가 필요합니다
(Drive도 같은 제약을 갖고 있습니다).

### 3-2. Resend (평가 알림)
스펙 12 · 5장의 "사이클 시작·마감 알림 이메일"을 아직 안 붙였습니다.
API 키가 없어서 지금 넣으면 **조용히 실패하는 코드만 남습니다**.
`/admin/reviews`에 "자동 발송되지 않습니다"라고 명시해 뒀습니다 —
사이클을 시작하면 임직원에게 직접 공지하셔야 합니다.

### 3-3. 스펙 07(전자계약) 차단 항목
- 모두싸인 API 키 · 웹훅 시크릿
- 근로계약서 · 연봉계약서 원본 양식
- 배포 도메인 (웹훅 수신 주소가 필요)

### 3-4. 스펙 08(증명서·법정교육) 차단 항목
- 증명서 PDF 양식 (재직·경력·원천징수)
- 법정교육 6종 확정 — 노무사 확인 필요
- PDF 생성 라이브러리 승인
- 스케줄러 방식 결정 (교육 이수 알림)

---

## 4. 다음 작업 전에 알아두셔야 할 함정

### 4-0. PostgREST 임베드에 테이블 이름만 적지 마세요
`employees!inner(name)`처럼 **테이블 이름만** 적으면 조회 전체가 죽습니다.

    Could not embed because more than one relationship was found

관계가 둘 이상으로 늘어나는 경로가 **두 가지**입니다.

**(1) 컬럼이 둘일 때.** `/welfare`와 `/admin/welfare`가 이것 때문에
"복지포인트 정보를 불러오지 못했습니다"만 띄우고 있었습니다
(`welfare_point_requests`가 `employee_id`와 `processed_by` 둘 다 갖고 있어서).
e2e 54건이 전부 통과하는 동안 못 잡았고 화면을 눌러보고서야 나왔습니다 —
검증들이 테이블을 컬럼 목록으로만 조회해서 임베드를 안 탔기 때문입니다.

**(2) 조인 테이블이 생겼을 때 — 컬럼이 하나여도 걸립니다.**
스펙 16에서 `community_members(board_id, employee_id)` 복합 PK 테이블이 생기자,
`boards`가 `employees`를 `created_by` **하나로만** 참조하는데도 모호해졌습니다.
PostgREST가 순수 조인 테이블을 보고 `boards ↔ employees` **다대다 관계를 한 벌 더
추론**하기 때문입니다.

```
후보 2개:
  many-to-one   boards_created_by_fkey using boards(created_by)
  many-to-many  community_members using ...board_id_fkey / ...employee_id_fkey
```

**결론: 컬럼이 하나뿐이라도 임베드에는 항상 FK를 명시하세요**
(`employees!boards_created_by_fkey(...)` 또는 `employees!컬럼명(...)`).
"지금은 하나니까 괜찮다"는 판단이 조인 테이블 하나에 무너집니다.
지금 코드베이스는 전부 명시 형태로 맞춰져 있고,
`scripts/e2e-community.ts` [7]절이 이 관계 후보가 **정확히 둘**인지 감시합니다 —
`archived_by` 같은 컬럼이 하나 더 붙으면 거기서 먼저 터집니다.

### 4-1. 스펙 08은 `employees` 컬럼 권한을 반드시 확인해야 합니다
`employees` 테이블은 RLS가 `select using(true)`로 열려 있고, 실제 방어선은
**컬럼 단위 GRANT**입니다. 스펙 08에서 새 컬럼(특히 `insurance_info` 같은
4대보험 정보)을 추가할 때 `authenticated`에 그냥 grant하면
**전 직원에게 열립니다**. 스펙 문서에는 이 얘기가 없습니다.

### 4-2. `text-title` 토큰이 `text-h1`과 같은 24px입니다
`tailwind.config.ts`의 `title`과 `h1`이 둘 다 24px/700입니다.
이름만 보고 "title은 h1보다 작겠지" 하고 쓰면 크기가 뒤집힙니다 —
위키 마크다운 제목에서 실제로 두 번 걸렸습니다. 정리할지 결정이 필요합니다.

---

## 5. 남은 기술 부채 (제가 처리할 것, 참고만)

- `calendar_events.attendee_ids` 배열 제거 (expand-contract의 contract 단계)
- nav 링크가 기간 파라미터를 유지할지 여부
- CSV 내보내기의 모수 질의를 버튼 클릭 시점으로 이동

---

## 이번에 끝난 것

| 스펙 | 화면 | 검증 |
|---|---|---|
| 09 업무일지·할일·목표 | `/worklog` `/todos` `/goals` | e2e 31건 + 세션 렌더 확인 |
| 10 프로젝트 | `/projects` `/projects/[id]` | 세션 렌더 확인 (캘린더 마일스톤 병합 포함) |
| 11 메일 연동 | 상단바 + 대시보드 위젯 | 파싱 9건 + CHECK 3건 + 세션 렌더 확인 |
| 12 업무평가 | `/reviews` 3화면 + 관리 | e2e 28건 |
| 13 자산·복지 | `/assets` 2화면 + `/welfare` + 관리 2화면 | e2e 54건 + 등록→신청→승인 전 구간 조작 확인 |
| 14 위키 | `/wiki` 4화면 | e2e 51건 + 작성→편집→렌더 확인 |

전 구간 `tsc 0 / eslint 0 / build 성공`. **e2e 197건 + 파싱 9건 + CHECK 3건.**

스펙마다 소넷 점검을 돌려 나온 지적(critical 1 · major 8 · minor 12)을 반영했고,
마이그레이션 적용 뒤 화면을 실제로 눌러 2건을 더 잡았습니다
(복지포인트 임베드 모호성 · 위키 링크의 괄호 잘림 — 둘 다 e2e가 못 잡던 자리).
각 커밋 메시지에 무엇이 어떻게 잘못됐었는지 적어뒀습니다.
